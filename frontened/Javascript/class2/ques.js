//ques 1
let foodPrice=1000;
let drinkPrice=200;

let totalBill=foodPrice+drinkPrice;
let tip=totalBill/10;
let finalAmount=totalBill+tip;

console.log(`the final amount including tip is ${finalAmount}`);

//ques 2
let originPrice=500;
let discount=originPrice*0.05;
let finalPrice=originPrice-discount;

console.log(`discounted price is ${finalPrice}`);

//ques 3
let productPice=1999;
let salesTaxRate=0.08;
let tax=productPice*salesTaxRate;
let total=productPice+tax;

console.log(`the total price is ${total}`)